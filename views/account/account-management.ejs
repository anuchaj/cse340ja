<main class="login-container">
    <h1><%= title %></h1>
  
    <% if (typeof message !== 'undefined' && message) { %>
        <div class="flash-message"><%= message %></div>
    <% } %>

    <h2 class="dashboard_text">You are logged in.</h2>
    
</main>